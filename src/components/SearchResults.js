import React from 'react';
import SearchResult from './SearchResult';

export default () => { // eslint-disable-line
  return (
    <React.Fragment>
      <SearchResult />
      <SearchResult />
      <SearchResult />
    </React.Fragment>
  );
};
